version: "3.9"

services:
  db:
    image: postgres
    environment:
      - POSTGRES_DB=pokemon
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
  web:
    build: .
    command: tail -f /dev/null
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    depends_on:
      - db
    environment:
      - DB_NAME=pokemon
      - DB_USER=postgres
      - DB_PASSWORD=postgres
      - DB_HOST=localhost
      #- DB_HOST=db
